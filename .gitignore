node_modules/
dist/
*.log
.DS_Store
.env
.env.local
.vscode/
.idea/
coverage/
.tsbuildinfo

# Pre-pivot code, kept locally for reference; not committed.
old/

# Vendored upstream MCP server (only needed if running locally instead of hosted endpoint).
vendor/

# electron-builder output (Mac app bundles, blockmaps, etc.). The lib's
# `dist/` lives at the repo root; we keep them separate.
release/

# Local `npm pack` tarballs — only generated when uploading a release
# asset by hand; the asset itself lives on the GitHub release page.
*.tgz
